name: standalone3d

services:
  server:
    build:
      context: ./server
      dockerfile: Dockerfile
      args:
        BACKEND_PORT_ARG: &bp $BACKEND_PORT
        CERTS_DIR_ARG: $CERTS_DIR
        TLS_CERT_LOC_ARG: $TLS_CERT_LOC
        TLS_KEY_LOC_ARG: $TLS_KEY_LOC
        DOMAIN_NAME_ARG: $DOMAIN_NAME
        ROOT_DIR_3D_ARG: $ROOT_DIR_3D
        HTTP_PORT_ARG: &hp $HTTP_PORT
        HTTPS_PORT_ARG: &hsp $HTTPS_PORT
        ADMIN_PORT_ARG: &ap $ADMIN_PORT
        CADDYFILE: $CADDYFILE
    ports:
      - "$HTTP_PORT:$HTTP_PORT"
      - "$HTTPS_PORT:$HTTPS_PORT"
      - "$ADMIN_PORT:$ADMIN_PORT"
    volumes:
      - docker_web:/srv
      - ./logs/:/cslt/logs
      - $CERTS_DIR:/certs
  frontend:
    build:
      context: .
      dockerfile: frontend-Dockerfile
      args:
        DOMAIN_NAME_ARG: localhost
        ROOT_DIR_ARG: .
        HTTP_PORT_ARG: *hp
        HTTPS_PORT_ARG: *hsp
        ADMIN_PORT_ARG: *ap
    volumes:
      - docker_web:/cslt/web
  backend:
    ports:
      - "$BACKEND_PORT:$BACKEND_PORT"
    build:
      context: ./backend
      dockerfile: Dockerfile
      args:
        BACKEND_PORT_ARG: *bp
    volumes:
      - ./logs/:/cslt/backend/logs
volumes: 
  docker_web:
    driver: local
